<Window x:Class="WpfApp1.Window7"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Просмотр TXT-файла"
        Height="520"
        Width="760"
        MinHeight="400"
        MinWidth="600"
        WindowStartupLocation="CenterScreen"
        Background="#151A25">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="18"
                CornerRadius="14"
                Background="#242C3C">
            <StackPanel>
                <TextBlock Text="Текстовый просмотрщик"
                           FontSize="25"
                           FontWeight="Bold"
                           Foreground="White"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1"
              Margin="0,18,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="160"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Margin="0,0,12,0"
                    Padding="12,8"
                    CornerRadius="8"
                    Background="#202838">
                <TextBlock x:Name="FileNameText"
                           Text="Файл ещё не выбран"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           Foreground="#D8E1F2"/>
            </Border>

            <Button Grid.Column="1"
                    Height="42"
                    Content="Открыть TXT"
                    Click="OpenFile_Click"
                    FontWeight="SemiBold"
                    Foreground="White"
                    Background="#3B82F6"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </Grid>

        <Border Grid.Row="2"
                CornerRadius="12"
                Background="#202838"
                Padding="2">
            <TextBox x:Name="FileTextBox"
                     Padding="16"
                     FontSize="16"
                     FontFamily="Consolas"
                     Background="#202838"
                     Foreground="#EEF3FF"
                     BorderThickness="0"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto" TextChanged="FileTextBox_TextChanged"/>
        </Border>
        <TextBlock x:Name="StatusText"
                   Grid.Row="3"
                   Margin="4,12,4,0"
                   Text=""
                   Foreground="#9AA9C4"/>
    </Grid>
</Window>
